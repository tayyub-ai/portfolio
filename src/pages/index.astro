---
import SiteLayout from "../components/SiteLayout.astro";
---

<SiteLayout
  title="Tayyub Yaqoob · Senior Digital Analyst · AI analytics, automation, measurement"
  description="UK-based senior digital analyst at 8 Million Stories. AI-assisted analytics, automation, and digital measurement engineering. Technical writing for Cointelegraph and AlphaWire, expert citations from CCN, peer reviews for IGI Global, and selected case studies."
  pathname="/"
  footerTitle="Work together or ask for closer detail."
  footerLead="Senior roles, consulting, expert commentary, conference talks, and reference checks. I reply directly from my inbox, usually within 48 hours on weekdays."
>
  <style slot="head">
    /* ── Hero ── */
    .hero-grid{align-items:center}
    .hero-grid h1{font-size:clamp(46px,6vw,86px);line-height:.98;letter-spacing:-.028em}
    .lede.lines{display:flex;flex-direction:column;gap:4px;font-size:16px;line-height:1.5;color:var(--ink-2);max-width:46ch}
    .lede.lines b{color:var(--ink);font-weight:600}
    .outlet-strip{display:flex;flex-wrap:wrap;align-items:center;gap:0;margin-top:28px;padding-top:18px;border-top:1px solid var(--line)}
    .outlet-strip .lab{font-family:var(--mono);font-size:9.5px;letter-spacing:.14em;color:var(--muted);text-transform:uppercase;margin-right:14px}
    .outlet-strip span:not(.lab){font-family:var(--serif);font-style:italic;font-size:17px;color:var(--ink-2);padding:0 14px;border-right:1px solid var(--line);transition:color .25s}
    .outlet-strip span:not(.lab):last-child{border-right:0}
    .outlet-strip span:not(.lab):hover{color:var(--terra)}
    @media (max-width:780px){
      .outlet-strip span:not(.lab){font-size:14px;padding:0 10px}
    }

    /* Portrait card */
    .portrait-card{position:relative;aspect-ratio:4/5;max-width:320px;margin-left:auto;border-radius:12px;overflow:hidden;background:var(--bg-soft);border:1px solid var(--line)}
    .portrait-card img{width:100%;height:100%;object-fit:cover}
    @media (max-width:880px){.portrait-card{margin:24px auto 0;max-width:280px}}

    /* ── Final CTA section ── */
    .closer{background:linear-gradient(160deg,#fff,var(--bg-soft));border:1px solid var(--line);border-radius:14px;padding:48px 52px;display:grid;grid-template-columns:1.4fr 1fr;gap:32px;align-items:end;box-shadow:var(--shadow-1)}
    .closer h2{font-family:var(--serif);font-weight:400;font-size:clamp(32px,3.8vw,52px);line-height:1.02;letter-spacing:-.02em;text-wrap:balance}
    .closer h2 em{font-style:italic;color:var(--terra)}
    .closer .lead{font-size:15px;color:var(--ink-2);line-height:1.65;margin-top:14px;max-width:54ch}
    .closer .right{display:flex;flex-direction:column;gap:18px;align-items:flex-end}
    .closer .right ul{list-style:none;display:flex;flex-direction:column;gap:6px;text-align:right;font-family:var(--mono);font-size:11px;color:var(--ink-2);letter-spacing:.04em;text-transform:uppercase}
    .closer .right ul li::after{content:"";display:inline-block;width:14px;height:1px;background:var(--terra);margin-left:8px;vertical-align:middle}
    @media (max-width:780px){.closer{grid-template-columns:1fr;padding:32px 26px}.closer .right{align-items:flex-start}.closer .right ul{text-align:left}.closer .right ul li::after{display:none}}

    /* Industry distribution bar */
    .indust-bar{display:flex;width:100%;height:18px;border-radius:9px;overflow:hidden;border:1px solid var(--line);margin-top:18px;background:#fff}
    .indust-bar span{display:block;height:100%;transform:scaleX(0);transform-origin:left;transition:transform 1.2s cubic-bezier(.4,0,.2,1)}
    .indust-bar.in span{transform:scaleX(1)}
    .indust-bar span:nth-child(1){background:#1a1714}
    .indust-bar span:nth-child(2){background:var(--terra)}
    .indust-bar span:nth-child(3){background:var(--ochre)}
    .indust-bar span:nth-child(4){background:var(--moss)}
    .indust-bar span:nth-child(5){background:var(--coral)}
    .indust-bar span:nth-child(1){transition-delay:.05s}
    .indust-bar span:nth-child(2){transition-delay:.15s}
    .indust-bar span:nth-child(3){transition-delay:.25s}
    .indust-bar span:nth-child(4){transition-delay:.35s}
    .indust-bar span:nth-child(5){transition-delay:.45s}
    .indust-key{display:flex;flex-wrap:wrap;gap:14px;margin-top:8px;font-family:var(--mono);font-size:10px;letter-spacing:.06em;color:var(--muted);text-transform:uppercase}
    .indust-key .k{display:inline-flex;align-items:center;gap:6px}
    .indust-key .sw{width:9px;height:9px;border-radius:2px;display:inline-block}

    /* Radial dial metric strip */
    .dials{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-top:28px}
    .dial{background:#fff;border:1px solid var(--line);border-radius:var(--r-md);padding:18px;display:flex;flex-direction:column;align-items:center;gap:6px;text-align:center;transition:transform .3s,border-color .3s,box-shadow .3s;position:relative;overflow:hidden}
    .dial::before{content:"";position:absolute;top:0;left:50%;transform:translateX(-50%);width:40%;height:2px;background:linear-gradient(90deg,transparent,var(--terra),transparent);opacity:0;transition:opacity .35s}
    .dial:hover{transform:translateY(-2px);border-color:var(--line-2);box-shadow:var(--shadow-1)}
    .dial:hover::before{opacity:1}
    .dial-svg{position:relative;width:110px;height:110px}
    .dial-svg svg{width:100%;height:100%}
    .dial .bg-ring{fill:none;stroke:var(--bg-soft);stroke-width:6}
    .dial .fg-ring{fill:none;stroke-width:6;stroke-linecap:round;stroke-dasharray:251;stroke-dashoffset:251;transform:rotate(-90deg);transform-origin:center;transition:stroke-dashoffset 1.6s cubic-bezier(.4,0,.2,1)}
    .dials.in .fg-ring{stroke-dashoffset:var(--off)}
    .dial.d-1 .fg-ring{stroke:var(--terra)}
    .dial.d-2 .fg-ring{stroke:var(--ochre)}
    .dial.d-3 .fg-ring{stroke:var(--moss)}
    .dial.d-4 .fg-ring{stroke:var(--coral)}
    .dial .num{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:var(--serif);font-size:30px;font-style:italic;color:var(--ink);letter-spacing:-.01em;line-height:1}
    .dial .num em{font-style:normal;font-size:18px;color:var(--terra);margin-left:1px}
    .dial .k{font-family:var(--mono);font-size:10.5px;letter-spacing:.06em;color:var(--muted);text-transform:uppercase;margin-top:4px}
    .dial .sub{font-size:12px;color:var(--ink-2);line-height:1.45;max-width:22ch}
    @media (max-width:880px){.dials{grid-template-columns:1fr 1fr}}

    /* ── Codex feature card ── */
    .codex{position:relative;display:grid;grid-template-columns:1.4fr 1fr;gap:32px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:var(--r-lg);padding:32px 36px;margin-top:8px;text-decoration:none;color:inherit;overflow:hidden;transition:transform .35s cubic-bezier(.2,.7,.1,1),box-shadow .35s,border-color .25s}
    .codex::before{content:"";position:absolute;left:0;top:0;width:4px;height:100%;background:var(--terra)}
    .codex:hover{transform:translateY(-3px);box-shadow:var(--shadow-2);border-color:var(--line-2)}
    .codex-tag{font-family:var(--mono);font-size:10.5px;letter-spacing:.1em;color:var(--muted);text-transform:uppercase}
    .codex-l h3{font-family:var(--serif);font-weight:400;font-size:clamp(34px,4vw,52px);line-height:1.02;letter-spacing:-.02em;color:var(--ink);margin:8px 0 14px}
    .codex-desc{font-size:15px;line-height:1.65;color:var(--ink-2);max-width:54ch;margin-bottom:18px}
    .codex-modules{list-style:none;padding:0;margin:0 0 22px;display:grid;grid-template-columns:1fr 1fr;gap:8px 18px}
    .codex-modules li{font-size:13px;color:var(--ink-2);line-height:1.5;padding-left:14px;position:relative}
    .codex-modules li::before{content:"";position:absolute;left:0;top:9px;width:6px;height:6px;border-radius:50%;background:var(--terra)}
    .codex-modules b{color:var(--ink);font-weight:600}
    .codex-cta{font-family:var(--mono);font-size:11px;letter-spacing:.08em;color:var(--terra);text-transform:uppercase;font-weight:600;display:inline-flex;align-items:center;gap:6px}
    .codex-r{display:flex;align-items:center;justify-content:center}
    .codex-viz{width:100%;max-width:320px}
    @media (max-width:880px){.codex{grid-template-columns:1fr;padding:24px;gap:18px}.codex-modules{grid-template-columns:1fr}.codex-r{order:-1}}

    /* ── Compact work cards with inline micro-viz ── */
    .work-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:8px}
    .work-card{position:relative;background:#fff;border:1px solid var(--line);border-radius:12px;padding:22px 24px;display:flex;flex-direction:column;gap:10px;text-decoration:none;color:inherit;transition:transform .3s cubic-bezier(.2,.7,.1,1),box-shadow .3s,border-color .25s}
    .work-card::before{content:"";position:absolute;left:0;top:0;width:3px;height:100%;background:var(--terra);transition:width .25s;border-radius:12px 0 0 12px}
    .work-card:hover{transform:translateY(-3px);box-shadow:var(--shadow-1);border-color:var(--line-2)}
    .work-card:hover::before{width:5px}
    .work-tag{font-family:var(--mono);font-size:10px;letter-spacing:.1em;color:var(--muted);text-transform:uppercase}
    .work-card h3{font-family:var(--serif);font-weight:400;font-size:22px;line-height:1.15;letter-spacing:-.01em;color:var(--ink);margin-top:2px}
    .work-card .card-viz{width:100%;height:auto;display:block;margin:6px 0 6px;border-bottom:1px dashed var(--line);padding-bottom:10px}
    .work-card p{font-size:13.5px;color:var(--ink-2);line-height:1.55;max-width:48ch;flex:1}
    .work-stats{display:flex;gap:18px;padding-top:10px;border-top:1px dashed var(--line);margin-top:6px;flex-wrap:wrap}
    .work-stats span{font-family:var(--mono);font-size:10px;letter-spacing:.06em;text-transform:uppercase;color:var(--muted);display:flex;flex-direction:column;gap:2px}
    .work-stats em{font-family:var(--serif);font-style:italic;font-size:18px;color:var(--ink);letter-spacing:-.01em;text-transform:none;line-height:1}
    @media (max-width:780px){.work-grid{grid-template-columns:1fr}}

    /* ── Feature quote (BBC) — editorial pull-quote ── */
    .quotes-feat{margin-top:8px}
    .quote-feature{position:relative;background:linear-gradient(180deg,#fff,var(--bg-soft));border:1px solid var(--line);border-radius:14px;padding:48px 64px 36px;margin:0;overflow:hidden}
    .quote-feature::before{content:"";position:absolute;left:0;top:0;width:4px;height:100%;background:var(--terra)}
    .quote-feature .quote-mark{position:absolute;top:-24px;left:48px;font-family:var(--serif);font-size:200px;line-height:1;color:var(--terra);opacity:.18;pointer-events:none;user-select:none}
    .quote-feature p{position:relative;z-index:1;font-family:var(--serif);font-style:italic;font-weight:400;font-size:clamp(24px,3vw,38px);line-height:1.22;letter-spacing:-.012em;color:var(--ink);margin:0 0 22px;text-wrap:balance;max-width:34ch}
    .quote-feature p .qhi{background:linear-gradient(180deg,transparent 62%,rgba(200,153,66,.4) 62%);padding:0 .08em}
    .quote-feature cite{display:flex;align-items:baseline;gap:18px;padding-top:18px;border-top:1px solid var(--line);font-style:normal;position:relative;z-index:1;flex-wrap:wrap}
    .quote-feature cite b{font-family:'Inter',sans-serif;font-size:15px;font-weight:700;color:var(--ink);letter-spacing:-.005em}
    .quote-feature cite span{font-family:var(--mono);font-size:10.5px;letter-spacing:.08em;color:var(--muted);text-transform:uppercase}
    @media (max-width:780px){.quote-feature{padding:36px 28px 28px}.quote-feature .quote-mark{left:18px;font-size:140px}}

    /* ── Small editorial quotes (press-clipping feel) ── */
    .quotes-row{display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;margin-top:14px}
    .quote-small{position:relative;background:#fff;border:1px solid var(--line);border-radius:10px;padding:22px 22px 18px;margin:0;display:flex;flex-direction:column;gap:14px;transition:transform .3s,border-color .25s}
    .quote-small:hover{transform:translateY(-2px);border-color:var(--line-2)}
    .quote-small::before{content:"\201C";position:absolute;top:8px;right:18px;font-family:var(--serif);font-size:64px;line-height:1;color:var(--terra);opacity:.18;pointer-events:none}
    .quote-small p{font-size:14px;line-height:1.55;color:var(--ink);margin:0;max-width:34ch}
    .quote-small p b{color:var(--ink);font-weight:600;background:linear-gradient(180deg,transparent 65%,rgba(200,153,66,.3) 65%);padding:0 .08em}
    .quote-small cite{display:flex;flex-direction:column;gap:2px;font-style:normal;padding-top:12px;border-top:1px dashed var(--line)}
    .quote-small cite .src{font-family:'Inter',sans-serif;font-size:13px;font-weight:600;color:var(--ink)}
    .quote-small cite .meta{font-family:var(--mono);font-size:9.5px;letter-spacing:.08em;color:var(--muted);text-transform:uppercase}
    .quote-small.q-gm{font-family:'Inter',sans-serif}
    .quote-small.q-dp p{font-family:var(--serif);font-style:italic;font-size:15px}
    .quote-small.q-mri{background:var(--bg-soft)}
    @media (max-width:780px){.quotes-row{grid-template-columns:1fr}}

    /* ── GitHub cards with contribution heatmap ── */
    .github-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:8px}
    .github-card{position:relative;background:#fff;border:1px solid var(--line);border-radius:12px;padding:22px 24px;display:flex;flex-direction:column;gap:10px;text-decoration:none;color:inherit;transition:transform .3s,box-shadow .3s,border-color .25s}
    .github-card:hover{transform:translateY(-3px);box-shadow:var(--shadow-1);border-color:var(--line-2)}
    .github-card .gh-head{display:flex;justify-content:space-between;align-items:center}
    .github-tag{font-family:var(--mono);font-size:10px;letter-spacing:.12em;color:var(--terra);text-transform:uppercase;font-weight:600}
    .gh-dot{width:8px;height:8px;border-radius:50%;background:var(--signal);box-shadow:0 0 0 0 rgba(90,138,74,.6);animation:pulse 2.4s infinite}
    .github-card h3{font-family:var(--mono);font-size:15px;font-weight:500;color:var(--ink);letter-spacing:-.005em}
    .github-card p{font-size:13.5px;color:var(--ink-2);line-height:1.55}
    .gh-heatmap{background:var(--bg-soft);border:1px solid var(--line);border-radius:8px;padding:10px 12px 8px;margin-top:auto}
    .gh-heatmap svg{width:100%;height:auto;display:block}
    .gh-legend{display:flex;justify-content:space-between;align-items:center;gap:8px;margin-top:6px;font-family:var(--mono);font-size:9px;letter-spacing:.06em;color:var(--muted);text-transform:uppercase}
    .gh-scale{display:inline-flex;gap:2px;align-items:center}
    .gh-scale i{width:8px;height:8px;border-radius:1.5px;background:var(--terra);display:inline-block}
    .github-cta{font-family:var(--mono);font-size:11px;letter-spacing:.06em;color:var(--terra);text-transform:uppercase;font-weight:600;padding-top:10px;border-top:1px dashed var(--line)}
    @media (max-width:780px){.github-grid{grid-template-columns:1fr}}

    /* Posters (legacy, unused after restructure) */
    .poster-strip{display:flex;flex-direction:column;gap:14px;margin-top:18px}
    .poster{position:relative;background:#fff;border:1px solid var(--line);border-radius:var(--r-lg);padding:24px;display:grid;grid-template-columns:1.5fr 1fr;gap:24px;align-items:stretch;overflow:hidden;transition:transform .35s cubic-bezier(.2,.7,.1,1),box-shadow .35s,border-color .25s;text-decoration:none;color:inherit}
    .poster:hover{transform:translateY(-3px);box-shadow:var(--shadow-2);border-color:var(--line-2)}
    .poster::before{content:"";position:absolute;left:0;top:0;width:4px;height:100%;background:var(--terra)}
    .poster .ptag{font-family:var(--mono);font-size:10.5px;letter-spacing:.1em;color:var(--muted);text-transform:uppercase}
    .poster .ph{font-family:var(--serif);font-weight:400;font-size:clamp(22px,2.4vw,32px);line-height:1.05;letter-spacing:-.015em;margin-top:6px;color:var(--ink)}
    .poster .pdesc{font-size:14px;color:var(--ink-2);line-height:1.6;margin-top:8px;max-width:50ch}
    .poster-viz{margin-top:14px;background:var(--bg-soft);border:1px solid var(--line);border-radius:12px;padding:14px;position:relative;overflow:hidden}
    .poster-viz svg{width:100%;height:auto;display:block}
    .pgutter{display:flex;flex-direction:column;justify-content:space-between;gap:14px;padding:6px 0}
    .pgutter .pstats{display:flex;flex-direction:column;gap:14px;padding-top:6px;border-top:1px dashed var(--line);margin-top:6px}
    .pstat{display:flex;flex-direction:column;gap:2px}
    .pstat .pv{font-family:var(--serif);font-size:26px;line-height:1;letter-spacing:-.01em;color:var(--ink)}
    .pstat .pv em{font-style:italic;color:var(--terra)}
    .poster.p-2 .pstat .pv em{color:#8a5e15}
    .poster.p-3 .pstat .pv em{color:var(--moss)}
    .pstat .pk{font-family:var(--mono);font-size:9.5px;letter-spacing:.08em;color:var(--muted);text-transform:uppercase}
    .pchips{display:flex;flex-wrap:wrap;gap:5px;margin-top:12px}
    .pchips span{font-family:var(--mono);font-size:9.5px;letter-spacing:.06em;text-transform:uppercase;padding:4px 8px;border-radius:999px;border:1px solid var(--line);background:#fff;color:var(--ink-2)}
    .pcta{display:inline-flex;align-items:center;gap:6px;font-family:var(--mono);font-size:10.5px;letter-spacing:.06em;color:var(--terra);text-transform:uppercase;margin-top:14px}
    .pcta::after{content:"→";transition:transform .25s}
    .poster:hover .pcta::after{transform:translateX(3px)}
    .pv-fc .grid line{stroke:var(--line);stroke-width:1;stroke-dasharray:2 4;opacity:.6}
    .pv-fc .axis-lbl{font-family:var(--mono);font-size:9px;fill:var(--muted);letter-spacing:.06em}
    .pv-fc .hist{stroke:var(--terra);stroke-width:2.4;fill:none;stroke-linecap:round;stroke-dasharray:600;stroke-dashoffset:600}
    .pv-fc .band{fill:var(--ochre);fill-opacity:.22;opacity:0;transition:opacity .9s ease 1s}
    .pv-fc .fc{stroke:var(--ochre);stroke-width:2;fill:none;stroke-dasharray:5 4;stroke-linecap:round;stroke-dashoffset:300}
    .pv-fc .now-line{stroke:var(--ink);stroke-width:1;stroke-dasharray:3 3;opacity:.6}
    .pv-fc .now-lbl{font-family:var(--mono);font-size:9px;fill:var(--ink);letter-spacing:.06em;text-transform:uppercase}
    .poster.in .pv-fc .hist{transition:stroke-dashoffset 1.6s cubic-bezier(.4,0,.2,1);stroke-dashoffset:0}
    .poster.in .pv-fc .fc{transition:stroke-dashoffset 1.4s cubic-bezier(.4,0,.2,1) 1.3s;stroke-dashoffset:0}
    .poster.in .pv-fc .band{opacity:1}
    .pv-rv .layer{fill:var(--line-2);transform-origin:bottom;transform:scaleY(0);transition:transform 1.1s cubic-bezier(.4,0,.2,1)}
    .pv-rv .layer.l-pos{fill:#a8c08a;transition-delay:.5s}
    .pv-rv .layer.l-neu{fill:#e7d6a8;transition-delay:.7s}
    .pv-rv .layer.l-neg{fill:#e8a995;transition-delay:.9s}
    .poster.in .pv-rv .layer{transform:scaleY(1)}
    .pv-rv .axis-lbl{font-family:var(--mono);font-size:9px;fill:var(--muted);letter-spacing:.06em}
    .pv-rv .flag-line{stroke:var(--ink);stroke-width:1;stroke-dasharray:3 3}
    .pv-rv .flag{font-family:var(--mono);font-size:8.5px;fill:var(--ink);letter-spacing:.04em}
    .pv-sp .panel{fill:#fff;stroke:var(--line);stroke-width:1}
    .pv-sp .panel-h{font-family:var(--mono);font-size:9px;fill:var(--muted);letter-spacing:.1em;text-transform:uppercase}
    .pv-sp .result{font-family:var(--sans);font-size:9px;fill:var(--ink-2)}
    .pv-sp .result-hit{fill:var(--moss);font-weight:600}
    .pv-sp .row{fill:#fff;stroke:var(--line);stroke-width:.8}
    .pv-sp .row-hit{fill:#fff5d7;stroke:var(--ochre)}
    .pv-sp .arrow{stroke:var(--terra);stroke-width:1.5;fill:var(--terra);stroke-dasharray:200;stroke-dashoffset:200;animation:none}
    .poster.in .pv-sp .arrow{animation:drawSparkArrow 1.4s cubic-bezier(.4,0,.2,1) .8s forwards}
    @keyframes drawSparkArrow{to{stroke-dashoffset:0}}
    .pv-sp .scrub-line{stroke:var(--line);stroke-width:1}
    .pv-sp .scrub-marker{fill:var(--terra)}
    .pv-sp .scrub-lbl{font-family:var(--mono);font-size:8.5px;fill:var(--muted);letter-spacing:.06em}
    @media (max-width:780px){.poster{grid-template-columns:1fr}.pgutter{flex-direction:row;align-items:end;flex-wrap:wrap}.pgutter .pstats{flex-direction:row;flex-wrap:wrap;border-top:0}}

    /* Press cell legacy styles (used by marquee) */
    .press-cell{padding:22px 16px;min-height:104px;gap:10px;align-items:center;text-align:center}
    .press-cell .pn-fallback{display:flex;align-items:center;justify-content:center;height:34px;line-height:1;letter-spacing:-.02em;font-weight:800;font-size:18px}
    .press-cell .pk{font-family:var(--mono);font-size:9.5px;letter-spacing:.16em;text-transform:uppercase;color:var(--muted);margin-top:2px}
    .brand-bbc .pn-fallback{font-family:Arial,sans-serif;font-weight:900;color:#000;letter-spacing:-1px}
    .brand-bbc .pn-fallback b{background:#000;color:#fff;padding:5px 6px;margin-right:2px;font-size:15px;display:inline-block;line-height:1}
    .brand-bbc .pn-fallback b:last-child{margin-right:0}
    .brand-tesco .pn-fallback{color:#ee1c2e;font-family:Helvetica,Arial,sans-serif;font-style:italic;font-weight:800;font-size:24px;letter-spacing:-.03em;position:relative;display:inline-flex;padding-bottom:3px}
    .brand-tesco .pn-fallback::after{content:"";position:absolute;left:0;right:0;bottom:0;height:2px;background:#0064b1;border-radius:1px}
    .brand-gm .pn-fallback{color:#0046ad;font-family:Inter,sans-serif;font-weight:800;font-size:17px;letter-spacing:-.02em}
    .brand-royal-london .pn-fallback{color:#1d3a6e;font-family:Georgia,serif;font-weight:600;font-size:18px;letter-spacing:.01em}
    .brand-nerdwallet .pn-fallback{color:#008a05;font-family:Inter,sans-serif;font-weight:800;font-size:18px;letter-spacing:-.01em}
  </style>

  <section class="hero wrap" data-screen-label="Intro" id="top">
    <div class="hero-grid">
      <div>
        <div class="eyebrow rv" data-parallax="6">Senior Digital Analyst · Stirling, United Kingdom</div>
        <h1 class="rv d1">
          Senior analyst, writer, and engineer working on AI, analytics, and measurement.
        </h1>
        <div class="hero-cta rv d3">
          <a href="/case-studies" class="btn btn-primary" data-magnet>See selected work →</a>
        </div>
        <div class="outlet-strip rv d4">
          <span class="lab">Selected work and citations</span>
          <span>Cointelegraph</span>
          <span>AlphaWire</span>
          <span>CCN</span>
          <span>IGI Global</span>
          <span>8 Million Stories</span>
        </div>
      </div>

      <div class="portrait-card rv d2">
        <img src="/assets/portrait.jpeg" alt="Tayyub Yaqoob" />
      </div>
    </div>
  </section>

  <section class="section wrap first" data-screen-label="Clients">
    <div class="sec-head rv">
      <div class="sec-head-l">
        <h2>Built for analyst and client teams at:</h2>
        <p>A selection of named environments where the work has been deployed.</p>
      </div>
    </div>

    <div class="press-marquee rv">
      <div class="press-marquee-track">
        <div class="press-cell brand-bbc"><span class="pn-fallback"><b>B</b><b>B</b><b>C</b></span><span class="pk">Media · UK</span></div>
        <div class="press-cell brand-tesco"><span class="pn-fallback">Tesco</span><span class="pk">Financial · UK</span></div>
        <div class="press-cell brand-gm"><span class="pn-fallback">General Mills</span><span class="pk">Consumer · global</span></div>
        <div class="press-cell brand-royal-london"><span class="pn-fallback">Royal London</span><span class="pk">Insurance · UK</span></div>
        <div class="press-cell brand-nerdwallet"><span class="pn-fallback">NerdWallet</span><span class="pk">Personal finance</span></div>
        <div class="press-cell brand-bbc" aria-hidden="true"><span class="pn-fallback"><b>B</b><b>B</b><b>C</b></span><span class="pk">Media · UK</span></div>
        <div class="press-cell brand-tesco" aria-hidden="true"><span class="pn-fallback">Tesco</span><span class="pk">Financial · UK</span></div>
        <div class="press-cell brand-gm" aria-hidden="true"><span class="pn-fallback">General Mills</span><span class="pk">Consumer · global</span></div>
        <div class="press-cell brand-royal-london" aria-hidden="true"><span class="pn-fallback">Royal London</span><span class="pk">Insurance · UK</span></div>
        <div class="press-cell brand-nerdwallet" aria-hidden="true"><span class="pn-fallback">NerdWallet</span><span class="pk">Personal finance</span></div>
      </div>
    </div>

    <div class="indust-bar rv">
      <span style="flex:0 0 35%"></span>
      <span style="flex:0 0 22%"></span>
      <span style="flex:0 0 18%"></span>
      <span style="flex:0 0 15%"></span>
      <span style="flex:0 0 10%"></span>
    </div>
    <div class="indust-key rv">
      <span class="k"><span class="sw" style="background:#1a1714"></span>Financial 35%</span>
      <span class="k"><span class="sw" style="background:var(--terra)"></span>Media 22%</span>
      <span class="k"><span class="sw" style="background:var(--ochre)"></span>Consumer 18%</span>
      <span class="k"><span class="sw" style="background:var(--moss)"></span>Insurance 15%</span>
      <span class="k"><span class="sw" style="background:var(--coral)"></span>Personal finance 10%</span>
    </div>

    <div class="dials rv">
      <div class="dial d-1" style="--off:75">
        <div class="dial-svg">
          <svg viewBox="0 0 90 90"><circle class="bg-ring" cx="45" cy="45" r="40"/><circle class="fg-ring" cx="45" cy="45" r="40"/></svg>
          <div class="num"><span data-count="35">0</span><em>+</em></div>
        </div>
        <span class="k">Technical articles</span>
        <span class="sub">Cointelegraph and AlphaWire combined, plus archive.</span>
      </div>
      <div class="dial d-2" style="--off:170">
        <div class="dial-svg">
          <svg viewBox="0 0 90 90"><circle class="bg-ring" cx="45" cy="45" r="40"/><circle class="fg-ring" cx="45" cy="45" r="40"/></svg>
          <div class="num"><span data-count="3">0</span></div>
        </div>
        <span class="k">IGI Global reviews</span>
        <span class="sub">Publisher-led peer-review record.</span>
      </div>
      <div class="dial d-3" style="--off:42">
        <div class="dial-svg">
          <svg viewBox="0 0 90 90"><circle class="bg-ring" cx="45" cy="45" r="40"/><circle class="fg-ring" cx="45" cy="45" r="40"/></svg>
          <div class="num"><span data-count="50">0</span><em>+</em></div>
        </div>
        <span class="k">Teams on systems</span>
        <span class="sub">Operational trust across recurring workflows.</span>
      </div>
      <div class="dial d-4" style="--off:130">
        <div class="dial-svg">
          <svg viewBox="0 0 90 90"><circle class="bg-ring" cx="45" cy="45" r="40"/><circle class="fg-ring" cx="45" cy="45" r="40"/></svg>
          <div class="num"><span data-count="12">0</span><em>+</em></div>
        </div>
        <span class="k">AI / ML credentials</span>
        <span class="sub">Google, AWS, LinkedIn Learning, related training.</span>
      </div>
    </div>
  </section>

  <section class="section wrap" data-screen-label="Product">
    <div class="sec-head rv">
      <div class="sec-head-l">
        <h2>Featured product.</h2>
        <p>What I work on day-to-day at 8 Million Stories.</p>
      </div>
      <div class="sec-head-r"></div>
    </div>

    <a class="codex rv" href="https://8ms.com/strategy-and-tech/codex" target="_blank" rel="noreferrer">
      <div class="codex-l">
        <span class="codex-tag">8 Million Stories · proprietary product</span>
        <h3>Codex.</h3>
        <p class="codex-desc">Competitive intelligence and technical performance monitoring for client teams, all in one dashboard.</p>
        <ul class="codex-modules">
          <li><b>Share of search</b> · market share vs competitors</li>
          <li><b>Competitor content alerts</b> · activity monitoring and analysis</li>
          <li><b>Technical Health Check</b> · live site-performance audit</li>
          <li><b>Carbon Footprint</b> · per-page environmental impact</li>
          <li><b>Data source monitoring</b> · ingest and update tracking</li>
        </ul>
        <span class="codex-cta">See Codex on 8ms.com ↗</span>
      </div>
      <div class="codex-r">
        <svg viewBox="0 0 280 220" preserveAspectRatio="xMidYMid meet" class="codex-viz">
          <rect x="20" y="20" width="240" height="180" rx="8" fill="#fff" stroke="var(--line)"/>
          <rect x="32" y="32" width="216" height="12" rx="2" fill="var(--bg-soft)"/>
          <rect x="32" y="56" width="60" height="60" rx="4" fill="var(--terra)" opacity=".15"/>
          <text x="62" y="92" font-family="var(--serif)" font-style="italic" font-size="16" fill="var(--terra)" text-anchor="middle">24%</text>
          <text x="62" y="106" font-family="var(--mono)" font-size="7" fill="var(--muted)" text-anchor="middle">SOS</text>
          <rect x="100" y="56" width="60" height="60" rx="4" fill="var(--terra)" opacity=".28"/>
          <text x="130" y="92" font-family="var(--serif)" font-style="italic" font-size="16" fill="var(--terra)" text-anchor="middle">42</text>
          <text x="130" y="106" font-family="var(--mono)" font-size="7" fill="var(--muted)" text-anchor="middle">ALERTS</text>
          <rect x="168" y="56" width="80" height="60" rx="4" fill="var(--terra)" opacity=".42"/>
          <text x="208" y="92" font-family="var(--serif)" font-style="italic" font-size="16" fill="#fff" text-anchor="middle">A+</text>
          <text x="208" y="106" font-family="var(--mono)" font-size="7" fill="#fff" text-anchor="middle">HEALTH</text>
          <rect x="32" y="128" width="216" height="60" rx="4" fill="var(--bg-soft)"/>
          <polyline points="44,170 80,158 116,164 152,148 188,156 224,140 236,148" stroke="var(--terra)" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
          <circle cx="236" cy="148" r="3" fill="var(--terra)"/>
        </svg>
      </div>
    </a>
  </section>

  <section class="section wrap" id="featured" data-screen-label="Case studies">
    <div class="sec-head rv">
      <div class="sec-head-l">
        <h2>Selected client work.</h2>
        <p>Four projects, with the headline numbers. The case studies page has each one written up in full.</p>
      </div>
      <div class="sec-head-r"><a href="/case-studies">All case studies →</a></div>
    </div>

    <div class="work-grid rv">
      <a class="work-card" href="/case-studies">
        <span class="work-tag">Forecasting · BigQuery · GA4</span>
        <h3>Channel and query forecasting.</h3>
        <svg class="card-viz" viewBox="0 0 300 60" preserveAspectRatio="none" aria-hidden="true">
          <path d="M180 24 Q215 14 250 16 T300 6 L300 36 Q260 38 225 40 T180 30 Z" fill="var(--ochre)" opacity=".22"/>
          <path d="M10 50 Q40 44 70 38 T130 30 T180 24" stroke="var(--terra)" stroke-width="1.8" fill="none" stroke-linecap="round"/>
          <path d="M180 24 Q215 14 250 16 T300 8" stroke="var(--ochre)" stroke-width="1.8" fill="none" stroke-dasharray="4 3" stroke-linecap="round"/>
          <line x1="180" y1="0" x2="180" y2="60" stroke="var(--ink)" stroke-width=".8" stroke-dasharray="2 3" opacity=".5"/>
          <text x="184" y="9" font-family="var(--mono)" font-size="7" fill="var(--ink)" letter-spacing=".08em">NOW</text>
          <circle cx="180" cy="24" r="3" fill="var(--terra)"/>
        </svg>
        <p>Monthly forecast cycles for GSC query trends and channel traffic. Variance flagged inside the cycle, not after.</p>
        <div class="work-stats">
          <span><em>5.4%</em>Median MAPE</span>
          <span><em>12</em>Runs / month</span>
          <span><em>4</em>Channels</span>
        </div>
      </a>

      <a class="work-card" href="/case-studies">
        <span class="work-tag">Sentiment · LLM-assisted</span>
        <h3>Sentiment and topic river.</h3>
        <svg class="card-viz" viewBox="0 0 300 60" preserveAspectRatio="none" aria-hidden="true">
          <path d="M0 60 L0 44 Q40 40 80 42 T160 38 T240 42 T300 40 L300 60 Z" fill="#e8a995" opacity=".75"/>
          <path d="M0 44 L0 28 Q40 24 80 27 T160 20 T240 25 T300 23 L300 40 Q260 42 240 42 T160 38 T80 42 T0 44 Z" fill="#e7d6a8" opacity=".8"/>
          <path d="M0 28 L0 10 Q40 6 80 8 T160 4 T240 6 T300 5 L300 23 Q260 24 240 25 T160 20 T80 27 T0 28 Z" fill="#a8c08a" opacity=".75"/>
          <line x1="160" y1="0" x2="160" y2="60" stroke="var(--ink)" stroke-width=".6" stroke-dasharray="2 3" opacity=".4"/>
          <text x="164" y="9" font-family="var(--mono)" font-size="7" fill="var(--ink)" letter-spacing=".06em">SPIKE</text>
        </svg>
        <p>LLM-assisted classification across press, review, and social. Nothing auto-published; analysts interpret context, not deltas.</p>
        <div class="work-stats">
          <span><em>11</em>Surfaces</span>
          <span><em>~3k</em>Mentions / wk</span>
          <span><em>4</em>Alert classes</span>
        </div>
      </a>

      <a class="work-card" href="/case-studies">
        <span class="work-tag">AI visibility · AI Overviews</span>
        <h3>AI vs traditional search.</h3>
        <svg class="card-viz" viewBox="0 0 300 60" preserveAspectRatio="none" aria-hidden="true">
          <rect x="0" y="4" width="140" height="52" rx="3" fill="#fff" stroke="var(--line)"/>
          <text x="6" y="14" font-family="var(--mono)" font-size="7" fill="var(--muted)" letter-spacing=".1em">GOOGLE</text>
          <rect x="6" y="18" width="128" height="6" fill="var(--line-2)" opacity=".55" rx="1"/>
          <rect x="6" y="28" width="128" height="6" fill="var(--line-2)" opacity=".55" rx="1"/>
          <rect x="6" y="38" width="128" height="6" fill="var(--terra)" opacity=".55" rx="1"/>
          <rect x="6" y="48" width="128" height="6" fill="var(--line-2)" opacity=".55" rx="1"/>
          <rect x="160" y="4" width="140" height="52" rx="3" fill="#fff" stroke="var(--line)"/>
          <text x="166" y="14" font-family="var(--mono)" font-size="7" fill="var(--muted)" letter-spacing=".1em">AI OVERVIEW</text>
          <rect x="166" y="18" width="128" height="6" fill="var(--terra)" opacity=".55" rx="1"/>
          <rect x="166" y="28" width="128" height="6" fill="var(--line-2)" opacity=".55" rx="1"/>
          <rect x="166" y="38" width="128" height="6" fill="var(--line-2)" opacity=".55" rx="1"/>
          <rect x="166" y="48" width="128" height="6" fill="var(--line-2)" opacity=".55" rx="1"/>
          <path d="M140 40 Q150 30 160 20" stroke="var(--coral)" stroke-width="1.2" stroke-dasharray="3 2" fill="none"/>
        </svg>
        <p>How brands surface across AI Overviews, ChatGPT, Perplexity, and conventional search. Each change captured with date, prompt, and citation.</p>
        <div class="work-stats">
          <span><em>140+</em>Queries</span>
          <span><em>4</em>Platforms</span>
          <span><em>Weekly</em>Cadence</span>
        </div>
      </a>

      <a class="work-card" href="/case-studies">
        <span class="work-tag">Reporting automation</span>
        <h3>Manual reporting → automated assembly.</h3>
        <svg class="card-viz" viewBox="0 0 300 60" preserveAspectRatio="none" aria-hidden="true">
          <text x="0" y="14" font-family="var(--mono)" font-size="7" fill="var(--muted)" letter-spacing=".1em">MANUAL</text>
          <rect x="44" y="6" width="76" height="12" fill="#d97757" rx="2"/>
          <rect x="120" y="6" width="58" height="12" fill="#c44d2c" rx="2"/>
          <rect x="178" y="6" width="60" height="12" fill="#c89942" rx="2"/>
          <rect x="238" y="6" width="36" height="12" fill="#7a8f5e" rx="2"/>
          <text x="280" y="16" font-family="var(--serif)" font-style="italic" font-size="11" fill="var(--ink)">4h</text>
          <line x1="0" y1="30" x2="300" y2="30" stroke="var(--line)" stroke-dasharray="2 3" opacity=".5"/>
          <text x="0" y="44" font-family="var(--mono)" font-size="7" fill="var(--muted)" letter-spacing=".1em">AUTO</text>
          <rect x="44" y="36" width="20" height="12" fill="var(--moss)" rx="2"/>
          <text x="70" y="46" font-family="var(--serif)" font-style="italic" font-size="11" fill="var(--moss)">20m</text>
        </svg>
        <p>Recurring reports and decks assembled from GA4, GSC, and ad-platform data. Multi-hour build replaced with a short analyst review.</p>
        <div class="work-stats">
          <span><em>36</em>Reports / mo</span>
          <span><em>~70%</em>Time saved</span>
          <span><em>Inline</em>Variance flags</span>
        </div>
      </a>
    </div>
  </section>

  <section class="section wrap" data-screen-label="Quotes">
    <div class="sec-head rv">
      <div class="sec-head-l">
        <h2>What clients have said.</h2>
        <p>Direct quotes from project teams and engagement partners.</p>
      </div>
      <div class="sec-head-r"></div>
    </div>

    <div class="quotes-feat rv">
      <blockquote class="quote-feature">
        <span class="quote-mark" aria-hidden="true">&ldquo;</span>
        <p>The tool is working fully, and we've been able to <span class="qhi">share the update with wider teams</span>.</p>
        <cite><b>BBC</b><span>Project stakeholder · media · UK</span></cite>
      </blockquote>
    </div>

    <div class="quotes-row rv d1">
      <blockquote class="quote-small q-gm">
        <p>"The feedback was very positive. Overall they are happy with the first go at this. <b>Passed on high praise of your work</b>."</p>
        <cite><span class="src">General Mills</span><span class="meta">Account team · Blue Buffalo</span></cite>
      </blockquote>
      <blockquote class="quote-small q-dp">
        <p>"Our analytics are <b>in great shape</b> at this point and we're feeling confident about the ways they will help us."</p>
        <cite><span class="src">Diligent Pharma</span><span class="meta">Stakeholder · life sciences</span></cite>
      </blockquote>
      <blockquote class="quote-small q-mri">
        <p>"I really liked working with you. <b>I have 3 to 4 clients that require your skills on a routine basis.</b>"</p>
        <cite><span class="src">MRI Management</span><span class="meta">Engagement partner · US</span></cite>
      </blockquote>
    </div>
  </section>

  <section class="section wrap" data-screen-label="Code">
    <div class="sec-head rv">
      <div class="sec-head-l">
        <h2>Code.</h2>
        <p>Personal projects on one profile; day-job contribution activity for 8 Million Stories on the other.</p>
      </div>
      <div class="sec-head-r"></div>
    </div>

    <div class="github-grid rv">
      <a class="github-card" href="https://github.com/tayyub-ai" target="_blank" rel="noreferrer">
        <div class="gh-head">
          <span class="github-tag">Personal</span>
          <span class="gh-dot" aria-hidden="true"></span>
        </div>
        <h3>github.com/tayyub-ai</h3>
        <p>Utilities, RAG and LLM experiments, multimodal research, and the portfolio source itself.</p>
        <div class="gh-heatmap" aria-label="12 weeks of contribution activity">
          <svg viewBox="0 0 252 76" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
            {Array.from({ length: 12 * 7 }).map((_, i) => {
              const week = Math.floor(i / 7);
              const day = i % 7;
              const seed = ((i * 9301 + 49297) % 233280) / 233280;
              const recency = week / 11;
              const intensity = Math.min(4, Math.floor(recency * 3 + seed * 2.8));
              const opacities = [0.08, 0.28, 0.5, 0.72, 0.95];
              return (
                <rect
                  x={week * 21 + 2}
                  y={day * 10 + 2}
                  width="18"
                  height="8"
                  rx="1.5"
                  fill="var(--terra)"
                  opacity={opacities[intensity]}
                />
              );
            })}
          </svg>
          <div class="gh-legend">
            <span>12 weeks</span>
            <span class="gh-scale" aria-hidden="true">
              <i style="opacity:.08"></i><i style="opacity:.28"></i><i style="opacity:.5"></i><i style="opacity:.72"></i><i style="opacity:.95"></i>
            </span>
            <span>active</span>
          </div>
        </div>
        <span class="github-cta">Open profile ↗</span>
      </a>

      <a class="github-card" href="https://github.com/Tayyab8ms" target="_blank" rel="noreferrer">
        <div class="gh-head">
          <span class="github-tag">8 Million Stories</span>
          <span class="gh-dot" aria-hidden="true"></span>
        </div>
        <h3>github.com/Tayyab8ms</h3>
        <p>Day-to-day analytics and product work. Repositories are private to the company; the contribution graph is public.</p>
        <div class="gh-heatmap" aria-label="12 weeks of contribution activity">
          <svg viewBox="0 0 252 76" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
            {Array.from({ length: 12 * 7 }).map((_, i) => {
              const week = Math.floor(i / 7);
              const day = i % 7;
              const seed = ((i * 1733 + 31019) % 233280) / 233280;
              const recency = week / 11;
              const intensity = Math.min(4, Math.floor(recency * 2.5 + seed * 3.2));
              const opacities = [0.08, 0.28, 0.5, 0.72, 0.95];
              return (
                <rect
                  x={week * 21 + 2}
                  y={day * 10 + 2}
                  width="18"
                  height="8"
                  rx="1.5"
                  fill="var(--terra)"
                  opacity={opacities[intensity]}
                />
              );
            })}
          </svg>
          <div class="gh-legend">
            <span>12 weeks</span>
            <span class="gh-scale" aria-hidden="true">
              <i style="opacity:.08"></i><i style="opacity:.28"></i><i style="opacity:.5"></i><i style="opacity:.72"></i><i style="opacity:.95"></i>
            </span>
            <span>active</span>
          </div>
        </div>
        <span class="github-cta">Open profile ↗</span>
      </a>
    </div>
  </section>


</SiteLayout>
